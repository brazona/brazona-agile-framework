management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
eureka:
  client:
    fetch-registry: 'false'
    register-with-eureka: 'false'
spring:
  application:
    name: eureka-server
server:
  port: '8761'
